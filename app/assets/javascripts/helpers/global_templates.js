Ember.Handlebars.registerHelper('humanDay', function(contentIndex) {
  //escaped = Handlebars.Utils.escapeExpression(value);
  //return new Handlebars.SafeString('<span class="highlight">' + escaped + '</span>');
  console.log(contentIndex);
  return 'test';
});
